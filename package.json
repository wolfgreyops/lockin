{
  "name": "lockin",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:cli": "pnpm --filter @lockin/cli dev",
    "dev:menubar": "pnpm --filter menubar dev",
    "build:web": "pnpm --filter web build",
    "build:cli": "pnpm --filter @lockin/cli build",
    "build:menubar": "pnpm --filter menubar build"
  }
}
